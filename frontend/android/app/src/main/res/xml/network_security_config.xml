<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!--
      Development: allow HTTP to localhost, emulator, and LAN IPs.
      Production builds should use HTTPS via API_BASE_URL.
    -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
